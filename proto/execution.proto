syntax = "proto3";

package execution;

message Order {
    string symbol = 1;
    string side = 2;
    int32 quantity = 3;
}

message Fill {
    string symbol = 1;
    float price = 2;
    int32 quantity = 3;
}

service SubmitOrder {
    rpc Submit (Order) returns (Fill) {}
}